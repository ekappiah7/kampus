import { color, brand, radius } from "@kampus/design-tokens";

export const theme = {
  color,
  brand,
  radius,
  quickLinkTint: [color.tint.gold, color.tint.blue, color.tint.pink, color.tint.green],
};
